{
  "iss": "https://pocket-oid.local",
  "sub": "${subject}",
  "aud": "${audience}",
  "scope": "${scope}",
  "iat": "${issued_at}",
  "exp": "${expires_at}",
  "jti": "${uuid}",
  "custom": {
    "tenant": "${metadata.tenant}",
    "env": "dev"
  }
}
